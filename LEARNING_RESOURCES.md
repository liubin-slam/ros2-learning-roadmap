# 学习资料入口

这个文件只放“应该去哪学”。学习时先看本文件，再进入对应项目 README。

## 第 0 周：环境

- ROS2 Humble 官方文档：搜索 `ROS2 Humble Installation Ubuntu`
- ROS2 官方 tutorials：搜索 `ROS2 Humble Tutorials`
- VS Code：搜索 `VS Code C++ Ubuntu`

## 第 1-2 周：C++ / Linux / CMake / Git

- C++：搜索 `菜鸟教程 C++ 类 文件 IO`
- CMake：搜索 `CMake Tutorial add_executable`
- Linux：搜索 `鸟哥 Linux 常用命令`
- Git：读 [GIT_BEGINNER.md](./GIT_BEGINNER.md)

重点只学：

- 变量、函数、类、struct
- 文件读写
- CMake 编译
- Git status/add/commit/push

## 第 3-4 周：ROS2 通信

- 搜索 `ROS2 Humble publisher subscriber C++`
- 搜索 `ROS2 Humble service client C++`
- 搜索 `ROS2 Humble parameters C++`
- 搜索 `ROS2 Humble launch file`

重点只学：

- node
- topic
- service
- parameter
- launch
- rosbag

## 第 5-6 周：嵌入式接口、URDF、TF

- 搜索 `Linux ttyUSB0 dmesg lsusb udev`
- 搜索 `python pyserial readline`
- 搜索 `ROS2 sensor_msgs Imu`
- 搜索 `ROS2 Humble URDF tutorial`
- 搜索 `ROS2 Humble TF2 tutorial`

重点只学：

- UART、USB、CAN、I2C、SPI 的用途
- `/dev/ttyUSB0` 权限排查
- IMU 数据发布
- link、joint、TF 树

## 第 7-8 周：仿真和导航

- 搜索 `ROS2 Humble Gazebo tutorial`
- 搜索 `Nav2 getting started ROS2 Humble`
- 搜索 `SLAM Toolbox ROS2 Humble`

重点只学：

- Gazebo/Gz 基础启动
- LaserScan
- 建图、定位、规划、控制的区别

## 第 9-10 周：视觉或点云

视觉：

- 搜索 `OpenCV Python VideoCapture`
- 搜索 `OpenCV HSV inRange findContours`
- 搜索 `ROS2 cv_bridge Python`

点云：

- 搜索 `ROS2 PointCloud2 PCL tutorial`
- 搜索 `PCL voxel grid filter`

## 第 11 周：驱动和通信

- 搜索 `Python socket TCP server client`
- 搜索 `TCP stream protocol buffer`
- 搜索 `ROS2 rclpy parameter publisher`

重点只学：

- 设备数据协议
- 超时、断连、重连
- 数据解析和非法数据处理

